# Sparse Arrays

HackerRank problem: Sparse Arrays

## Solution
The program counts how many times each string occurs in the given string list. For every query, it returns the corresponding frequency.

## Complexity
- Time Complexity: O(N + Q)
- Space Complexity: O(N)

Language: C++
